{
  "name": "postmill-extension",
  "version": "2.0.0",
  "description": "Postmill browser extension for cookie-based platform authentication",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "rm -rf dist && vite build && cp manifest.json dist/manifest.json && cd dist && zip -r ../extension.zip .",
    "dev": "rm -rf dist && NODE_ENV=development dotenv -e ../../.env -- vite build --config vite.config.chrome.ts --mode development --watch",
    "test": "vitest run"
  },
  "type": "module",
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@vitejs/plugin-react": "^6.1.0",
    "dotenv-cli": "^8.0.0",
    "postcss": "^8.5.10",
    "vite": "^8.2.2",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  }
}
